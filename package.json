{
  "name": "cli-seo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build-deploy": "node ./build.js",
    "server-install": "cd server && npm install && cd ..",
    "start-deploy": "npm run build-deploy && nodemon ./deploy/server/app.js"
  },
  "author": "John Livingston",
  "license": "ISC",
  "devDependencies": {
    "fs-extra": "^4.0.1"
  }
}
